# Copiar a `.env` y completar (no commitear `.env`).
# Pooler: runtime Next.js / PrismaClient (`src/lib/prisma.ts`).
DATABASE_URL="postgresql://USER:PASSWORD@HOST-pooler.REGION.aws.neon.tech/neondb?sslmode=require"

# Conexión directa: CLI Prisma migraciones (`prisma.config.ts` usa DIRECT_URL si existe).
DIRECT_URL="postgresql://USER:PASSWORD@HOST.REGION.aws.neon.tech/neondb?sslmode=require"

# --- Sesión / editor (producción obligatorio) ---
SESSION_SECRET="cambia-por-32-bytes-aleatorios-en-prod-OK"
ADMINISTRADOR_PASSWORD="clave para activar modo editor en UI"
# Alias legado (si ADMINISTRADOR_PASSWORD está vacío se usa este).
# EDITOR_PASSWORD=""

# --- DUX API (lista tienda y compras; producción según entorno) ---
DUX_API_TOKEN=""
# Timeout por intento HTTP (fetch + body). No incluye esperas de reintento 429.
# Default en código: 30000. Si ves timeouts frecuentes con catálogo grande, subir a 45000–60000.
DUX_FETCH_TIMEOUT_MS="30000"
DUX_COD_TIENDA="DUX"
DUX_SYNC_DELAY_MS="5000"
# ID lista DUX principal (default 56994) → prod_tienda_precios
DUX_ID_PRECIO_LISTA="56994"
# IDs depósito DUX Maipú / Guaymallén (default 16923 / 4565) para parser sync / stockeable.
# El mapeo sucursal → depósito vive en `global_sucursales.id_deposito` (debe coincidir).
DUX_ID_STOCK_MAIPU="16923"
DUX_ID_STOCK_GUAYMALLEN="4565"
DUX_COMPRAS_SYNC_LIMIT=""
DUX_COMPRAS_SYNC_MAX_PAGES=""
DUX_COMPRAS_MIN_INTERVAL_MS=""
DUX_ID_EMPRESA_COMPRAS=""
# Mismo idEmpresa para GET /v2/remitos-venta (Fact & Cobros).

# --- Otros ---
# Deprecado en runtime: cotización USD vive en `global_cotizacion_usd` (ver BACKEND_GUIDELINES §3.2).
# Solo seed/fallback si la tabla está vacía al primer acceso.
COTIZACION_DOLAR="1"

# --- Google Sheets (export / probe; service account) ---
# En producción también van en Vercel → Environment Variables.
GOOGLE_SERVICE_ACCOUNT_EMAIL=""
# Pegar private_key del JSON; Vercel suele aceptar `\n` literales en una línea.
GOOGLE_PRIVATE_KEY=""
GOOGLE_SHEETS_SPREADSHEET_ID=""

# --- ARCA / AFIP (WSAA + WSFEv1). Homologación por defecto. No commitear PEM. ---
# homo | prod  (default homo si vacío)
ARCA_ENV="homo"
# Timeout SOAP ms (default 30000).
ARCA_TIMEOUT_MS="30000"
# Tope ImpTotal para Consumidor Final sin identificar (DocTipo 99 / DocNro 0).
ARCA_CF_MAX_SIN_DOC="10000000"
# Un par por CUIT emisor. En Vercel: Production y Preview. No reabrir y guardar vacío.
ARCA_CERT_PEM_20372672235=""
ARCA_KEY_PEM_20372672235=""
ARCA_KEY_PASSPHRASE_20372672235=""
ARCA_CERT_PEM_23169084289=""
ARCA_KEY_PEM_23169084289=""
ARCA_CERT_PEM_20329808824=""
ARCA_KEY_PEM_20329808824=""
# Fallback de un solo emisor (si no hay variables con CUIT).
# ARCA_CERT_PEM=""
# ARCA_KEY_PEM=""
# ARCA_CUIT=""

